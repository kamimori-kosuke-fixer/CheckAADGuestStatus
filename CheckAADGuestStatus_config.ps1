#各種Init
#ループ時間（秒単位）
$global:waitSecond_default = 60

#WebhookのURL(Slack API[https://api.slack.com/apps/{AppID}/incoming-webhooks]から取得)
$global:Webhook_default = ""

#通知に利用するアイコンのURI(任意)
$global:image_default = ""
